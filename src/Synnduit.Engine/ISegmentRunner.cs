namespace Synnduit
{
    /// <summary>
    /// Performs individual run segment runs.
    /// </summary>
    internal interface ISegmentRunner
    {
        /// <summary>
        /// Performs the current run segment run.
        /// </summary>
        void Run();
    }
}
